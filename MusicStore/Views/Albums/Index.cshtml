@model List<Album>

	<h1>Albums</h1>

	<ul>
	@foreach (var album in Model)
	{
		<li>
			<a asp-controller="Albums" asp-action="Details" asp-route-id="@album.Id">@album.Name</a>
		</li>
	}
</ul>
